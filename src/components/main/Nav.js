import NavEl from './NavEl';
const Nav = ({projectList}) => {
    const navContent = projectList.map(project=>(
    ))
  return (
    <div>
      <h1>Navbar</h1>
    </div>
  );
};

export default Nav;
